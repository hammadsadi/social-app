export const signUp = async (req, res) => {
  res.json({ data: " Controllers SignUp" });
};

export const signin = async (req, res) => {
  res.json({ data: " Controllers Signin" });
};

export const signOut = async (req, res) => {
  res.json({ data: " Controllers Signout" });
};
